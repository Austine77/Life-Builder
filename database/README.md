# Database folder

Life Builder uses **MongoDB Atlas** as its database.

This folder contains:
- `init.mongodb.js` for optional manual collection and index setup in mongosh.
- `collections.md` describing the collections used by the backend.

The backend already creates models and indexes automatically through Mongoose, so this folder is support material rather than a second application.
